import 'package:flutter/material.dart';

// some code to upload file on github

class SomeWidget extends StatelessWidget {
  const SomeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
